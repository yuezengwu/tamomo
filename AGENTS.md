# TAMOMO — Agent 工作规则

开始工作前先阅读 `README.md` 和当前代码，保留用户已有修改。项目默认使用中文。

## 持续学习闭环

每项工作都遵循：

1. **读取**：复用现有代码、实验、决定和主要来源，不重复已经完成的研究。
2. **假设**：明确当前要回答的问题，以及什么结果会推翻判断。
3. **实验**：选择最小、可逆、可复现的实现或实验。
4. **验证**：与单机或既有基线比较，检查收敛、故障、时间、能耗和网络成本。
5. **记录**：保留命令、配置、原始结果、失败和限制；稳定知识进入正式文件。
6. **进化**：新证据与旧判断冲突时更新判断、文档和下一步，不维护已经失效的结论。

## 边界

- 区分事实、推断、第一方声称和未来设想。
- 使用最简单方案，不提前增加平台、流程和抽象。
- `tmp/` 仅限本地并由 Git 忽略；已跟踪文件不得依赖其中内容。
- 不提交秘密或个人信息，不未经同意增加遥测、大型下载或任意远程代码执行。
- 未经用户明确授权，不发布、购买、创建外部资源或执行其他对外动作。

